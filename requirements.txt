# ─────────────────────────────────────────────
# fresh-kitchen-ai-server — Python 3.10.19
# ─────────────────────────────────────────────
# 직접 의존성만 명시합니다. transitive 의존성(starlette, numpy, grpcio 등)은
# pip가 자동으로 해결합니다.

# 웹 서버
fastapi==0.136.1
uvicorn==0.46.0
python-multipart==0.0.27   # FastAPI File 업로드용

# 환경 변수
python-dotenv==1.2.2

# 딥러닝 (Food Classifier 추론 / 학습)
torch==2.11.0
torchvision==0.26.0

# 이미지 처리
pillow==12.2.0

# Google Cloud (영수증 OCR) + Gemini (3개 파이프라인 공용)
google-cloud-documentai==3.14.0
google-genai==1.75.0

# 이미지 크롤러 (scripts/data_crawl.py)
ddgs==9.14.4
requests==2.33.1
